package:
  name: cronmon
  version: "0.1.0"

build:
  script: python setup.py install

requirements:
  build:
    - python
  run:
    - python

source:
  path: ../

about:
  home: https://github.com/malev/cronmon
